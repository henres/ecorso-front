import {Link} from 'react-router-dom';
import {Button} from "@blueprintjs/core";
import * as React from "react";

interface NavLinkInterface {

}

class AlertButton extends Button {
    render () {
        return(
            <button>reqreqw</button>
        )
    }
}
